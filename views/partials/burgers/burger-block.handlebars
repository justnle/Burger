<li class="d-flex align-items-center justify-content-between py-1">
    {{burger_name}}

    <button type="button" class="btn {{#if devoured}}btn-warning{{else}}btn-success{{/if}} change-devour"
        data-id="{{id}}" data-devoured="{{devoured}}">
        {{#if devoured}}TRASH{{else}}EAT{{/if}}
    </button>
</li>